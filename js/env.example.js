/**
 * ADECOMPT - Configuração Local (apenas desenvolvimento)
 *
 * INSTRUÇÕES:
 * 1. Copie este arquivo como "env.js" na raiz do projeto
 * 2. Preencha com suas credenciais do Supabase
 * 3. NUNCA commit o arquivo env.js (está no .gitignore)
 *
 * No Cloudflare Pages, este arquivo é gerado automaticamente
 * pelo script build.js usando variáveis de ambiente.
 */

window.ADECOMPT_CONFIG = {
  supabaseUrl: 'https://seu-projeto.supabase.co',
  supabaseAnonKey: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.sua-chave-aqui'
};
